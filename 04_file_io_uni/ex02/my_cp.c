/* Write a program like cp that, when used to copy a refular file that contains 
 * holes (sequence of null bytes), also creates corresponding holes in the
 * target file.
 */

/* Usage: ./my_cp.exe target_file.txt source_file.txt */

#include "tlpi_hdr.h"

int main() {
	return (EXIT_SUCCESS);
}